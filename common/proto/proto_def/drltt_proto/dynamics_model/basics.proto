syntax = "proto3";

package drltt_proto;

// State coordinates in fixed space frame.
message BodyState {
    // X-coordinate in [m].
    optional float x = 1;
    // Y-coordinate in [m].
    optional float y = 2;
    // Orientation/heading in [rad].
    optional float r = 3;
}
